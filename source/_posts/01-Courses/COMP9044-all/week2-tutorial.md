---
title: week2-tutorial
date: 2026-03-10T14:40:15+11:00
tags:
categories: COMP9044
---


# 第二阶段： 核心工具笔记

## 1. sort  -排序

sort : 不仅仅是排序， 关键在于 ```-k(key)``` 定位字段和 ```-n(numeric)```按排序排

| 选项                       | 含义                       | 示例        |
| -------------------------- | -------------------------- | ----------- |
| -k F,F(F表示 Field， 字段) | 按第 F 个字段排序          | sort -k2,2  |
| -t 'SEP'                   | 按照指定分隔符排序         | sort -t '   |  | ' |
| -n                         | 数值排序(非字典序)         | sort -k3,3n |
| -r                         | 逆序排序                   | sort -k2,2r |
| -k F.c                     | 从第F个字段的第c个字符开始 | -k1.8,1n    |

### 多键排序

```sort -k3,3 -k2,2 benchmarks```


⚠️ -k2,2 和 -k2 的区别：-k2 表示"从第2字段到行尾"，-k2,2 才表示"仅第2字段"。做精确排序时要加结束列号!

---

**练习1-A**
下面的文件是 benchmarks（Tab 分隔），第2列是"第一基准"，第3列是"第二基准"，第4列是"第三基准"。
写命令：先按第三基准排序，再按程序编号（字段1中数字部分）排序。

| 基准1     | 基准2 | 基准3 | 基准4 |
| --------- | ----- | ----- | ----- |
| program1  | 08    | 03    | 05    |
| program10 | 09    | 04    | 16    |
| program2  | 14    | 03    | 05    |

**参考答案**

```shell

sort -k4,4 -k1.8,1n benchmarks.txt

```


-k1.8,1n 表示从第1个字段的第8个字符("Program"后面的数字开始)， 按照数值排序。 这样Program2就排在Program10的前面了

---

**练习1-B**
有一个管道符分隔的成绩文件 marks.psv（格式：zID|mark），如何按 zID 排序并保存到 marks-sorted.psv？


**参考答案**
```shell
sort -t '|' -k1,1 marks.psv > marks-sorted.psv
```
{% asset_img image.png alt text %}

---

## 2. cut -提取字段/字符

垂直切割。 用 ```-d``` 指定分隔符， 

| 选项                        | 含义                       |
| --------------------------- | -------------------------- |
| -d 'SEP'                    | 指定字段分隔符（默认Tab）  |
| -f N(N表示Number 数字/索引) | 提取第 N 个字段（从1开始） |
| -f 1,3                      | 提取第1和第3字段           |
| -c N                        | 提取第 N 个字符            |

**例如: 从 /etc/passwd/ 提取用户名：(第1字段， 冒号分隔)**

```shell
cut -d':' -f1 /etc/passwd
```

**再比如：提取用户名和密码，然后换成Tab分隔符**
```shell
cut -d':' -f1,2 /etc/passwd | tr ':' '\t' > passwords.txt
```
---

**练习2-A**

给定 /etc/passwd，请写命令只输出所有用户的家目录（第6字段）。

**参考答案**

```shell
cut -d':' -f6,6 /etc/passwd
```
---

## 3. tr - 字符转换 /删除 / 压缩

**核心用法**
| 选项       | 含义                               |
| ---------- | ---------------------------------- |
| tr 'A' 'B' | 把字符集A中的字符替换为B中对应字符 |
| -d 'SET'   | 删除 SET 中的字符                  |
| -c 'SET'   | 取补集（SET 以外的所有字符）       |
| -s 'SET'   | 压缩（squeeze）连续重复字符为一个  |
| [:alpha:]  | 所有字母（等同 a-zA-Z）            |
| [:alnum:]  | 所有字母+数字                      |
| [:lower:]  | / [:upper:]	小写 / 大写字母        |

---
**经典技巧**

```shell
tr -cs 'a-zA-z0-9' '\n' < somefile
```

解析: ```-c``` 取补集， 把所有的非字母数字字符替换为 \n ; -s 压缩连续换行， 每行一个词

---

**流水线示例**

输入文本: this is big Big BIG\nbut this is not big

```shell

#统计各词出现的次数(不区分大小写)

tr -cs '[:alpha:]' '\n' | tr '[:lower:]' '[:upper:]' | sort | uniq -c

4 BIG
1 BUT
2 IS
1 NOT
1 SO
2 THIS

```
---


**练习 3-A**
用 ```tr``` 把文件中所有大写字母改成小写， 并且删除所有数字， 写出单挑命令(可用管道)

```shell
tr '[:lower:]' '[:upper:]' < file | tr -d '[:digit:]'
```

---

**练习3-B**

以下命令的输出有几行

```shell
echo "hello world foo bar" | tr -d ' ' | wc -w

```

**参考答案**

只有1个词， ```tr -d``` 把 ' ' 把空格都删掉， 四个词变成一个长词

---

## 4. grep - 行过滤

---

核心用法

| 选项 | 含义                          |
| ---- | ----------------------------- |
| -E   | 扩展正则(ERE，支持 + ? {} ()) |
| -v   | 取反（显示不匹配的行）        |
| -i   | 忽略大小写                    |
| -c   | 只输出匹配行数                |
| ^    | 行首                          |
| $    | 行尾                          |
| \b   | 单词边界                      |

---

   显示以cs/se/bi/en 开头 + 4位数字的帐户行
   ```shell
   grep -E '^(cs|se|bi|en)[0-9]{4}:' /etc/password
   ```



   提取shell 是 /bin/bash 的用户名
   ```shell
   grep -E ':/bin/bash' /etc/passwd | cut -d':' -f1
   ```
---

**练习**

从 /etc/passwd 中找出所有没有密码(密码字段为空， 即两个冒号相邻```::```的账户， 只显示用户名)

**答案**
```shell
grep -E '::' /etc/passwd | cut -d':' -f1
```
---

**练习**

从 /etc/passwd 找出UID(第3字段) 大于等于1000的普通用户， 写出命令(提示: 先用grep 过滤，在考虑数值范围)
**答案**
```shell
awk -F: '$3 >= 1000' /etc/passwd
```
纯用 grep/cut 很难做数值比较，此时 awk 更合适。
这是一个很好的边界案例：知道 grep 的局限，才知道什么时候换工具。

---


## 5. sed  - 流编辑器

| 命令                    | 用法                         |
| ----------------------- | ---------------------------- |
| s/PATTERN/REPLACEMENT/  | 替换(1次)                    |
| s/PATTERN/REPLACEMENT/g | 替换(全行)                   |
| /PATTERN/d              | 删除                         |
| /START/,/END/d          | 删除从 START 到 END 的行范围 |
| -E                      | 使用扩展正则                 |
| \t                      | Tab字符（在模式中）          |


   ```shell
   #去掉 Tab 后的前导0: \t0 -> \t(全局替换)
   sed -Ee 's/\t0/\t/g' benchmarks



   #删除 progarm2 到 program13 之间的行
   sed -Ee '/^program\b/,/^program13\b/d' benchmarks
   ```
⚠️ \b 是单词边界，确保 program2 不会误匹配 program20。

---

   sed -n


这是个非常关键的参数。简单来说，-n 的作用是**“关掉 sed 的大喇叭”**。
1. 默认情况：Sed 是个“复读机”默认情况下，sed 每读入一行，处理完之后无论有没有修改，都会把这一行自动打印出来。Bash# 即使你什么都不做，它也会把整个文件原样打出来
sed '' data.txt
2. 加入 -n：Sed 变成“沉默者”当你加上 -n（全称 --quiet 或 --silent）时，sed 进入静默模式。它依然会读取每一行，但除非你明确命令它打印，否则它什么都不会输出。Bash# 什么都不会显示，因为没有打印指令
sed -n '' data.txt 
3. -n 必须配合 p 指令使用在静默模式下，我们通常使用 p (print) 指令来告诉 sed：“只有符合这个条件的行，才给我印出来！”    这就是为什么打印第 3 到 7 行要写成：Bashsed -n '3,7p' data.txt.    -n：先闭嘴，不要默认打印所有行。   3,7：找到第 3 到第 7 行。p：把这几行印出来。

4. 如果不加 -n 会发生什么？如果你只写 sed '3,7p' data.txt（没加 -n），结果会让你大吃一惊：第 1 行：不是 3-7，默认打印一次。第 2 行：不是 3-7，默认打印一次。第 3 行：属于 3-7，执行 p 指令打印一次，然后 sed 默认又打印一次（这行会出现两次！）。...以此类推。

结果如下：Plaintextline1
line2
line3  <-- p 指令印的
line3  <-- 默认印的
line4
line4
...
💡 形象的比喻普通 sed：像个复读机，不管改没改，每行都得喊一遍。sed -n：像个狙击手，潜伏在那不动，只有看到目标（匹配到模式）并接到指令（p）时才开火（打印）。常见用法总结命令含义sed -n '5p'只打印第 5 行sed -n '$p'只打印最后一行sed -n '/error/p'只打印包含 "error" 关键字的行（类似 grep）


---

我们要把它们分成两大阵营：一个是运行 sed 的外部开关，另外两个是写在引号里的内部指令。
1. 外部开关：-i (In-place)它是 sed 命令的一个选项，写在引号外面。
   它的作用只有一个：直接修改原文件。
   默认（不加 -i）：sed 只是把处理后的结果印在屏幕上给你看，原文件没有任何变化。
   加了 -i：sed 会悄悄地把修改保存回原文件。[!WARNING]小心： 在 macOS 上，必须写成 sed -i '' '...'（多一个空引号）。

2.  内部指令：a 和 i这两个是写在引号里的动作指令，用来在文件的特定位置增加新行。
   a (Append) —— 在“后”加逻辑：在匹配到的行之后插入新内容。口诀：After（在……之后）。
   i (Insert) —— 在“前”加逻辑：在匹配到的行之前插入新内容。口诀：In front of（在……前面）。


3.  三者对比表符号身份作用位置例子-i选项 (Option)保存到文件引号外面sed -i '...' filea指令 (Command)在行后追加引号内面sed '2a\Hello' filei指令 (Command)在行前插入引号内面sed '2i\Hello' file



符号,身份,作用,位置,例子
-i,选项 (Option),保存到文件,引号外面,sed -i '...' file
a,指令 (Command),在行后追加,引号内面,sed '2a\Hello' file
i,指令 (Command),在行前插入,引号内面,sed '2i\Hello' file


---


**练习 5-A**
写sed命令: 把文件中所有http:// 替换成 https://

```shell
sed -Ee 's|http://|/https://|g' file

# 这里用 | 作为分隔符，避免在模式中转义 /。
```
---
   还有哪些字符可以当定界符？
   理论上，你可以使用任何非换行、非空格的字符。常见的替代方案有：

   使用 #（也非常常用）：

   ```shell
   sed 's#http://#https://#g' file
   ```
   使用 :（适合路径中不包含冒号的情况）：

   ```shell
   sed 's:http://:https://:g' file
   ```

   使用 @：
   ```shell
   sed 's@http://@https://@g' file
   ```
   1. 一个进阶的“坑”：防止重复替换
      如果你直接运行 ```sed 's|http://|https://|g'```，而文件中已经存在一些 https:// 的链接，会出现什么后果？

      输入： ```https://google.com```

      匹配结果： ```http:// 部分会被匹配，替换成 https://。```

      最终输出： ```hhttps://google.com（多了一个 h！）```

      为了写出更稳健（Professional）的命令，你可以加一个“负向约束”，只匹配后面不带 s 的 http：

      匹配 http 且后面不是 s 的地方（这里用了扩展正则 -E）
      ```shell
      sed -E 's|http://([^s])|https://\1|g' file
      ```

      或者更简单的逻辑：先全换成 https，再把 hhttps 换回 https
      ```shell
      sed 's|http://|https://|g; s|hhttps://|https://|g' file
      ```

      **总结**
      你提到的用 | 分隔的写法非常地道。在编写脚本时，根据处理的内容灵活更换定界符，能极大地提高代码的可读性。

---

**练习 5-B**

写sed命令: 删除文件中所有空行

```shell
sed '/^$/d' file
```
   1. 为什么 \s 在 sed 里可能失效？
      sed 主要分为两个流派，它们对 \s 的支持完全不同：

      GNU sed (Linux/WSL2)： 通常支持 \s 匹配空格或 Tab。

      BSD sed (macOS)： 不支持 \s。如果你在 Mac 的终端写 sed 's/\s//g'，它可能根本不理你，或者把你文本里的字母 s 给删了。

      更稳妥的写法：
      为了保证在任何机器上都能运行，大家习惯用 [[:space:]]，它等价于 \s（匹配空格、制表符、换页符等）。

   2. \s 能代表“空行”吗？
      这是一个常见的误区：\s 代表的是“字符”，而“空行”是“逻辑状态”。

      \s：匹配一个看不见的字符（空格或 Tab）。

      ^$：匹配一个位置（行首紧跟行尾），这才是真正的空行。

      ^\s*$：匹配**“伪空行”**（行内可能包含空格或 Tab，但没有文字）。

      如果你想删除所有“看似空着”的行，最强大的写法是：

      ```shell
      sed '/^[[:space:]]*$/d' file
      ```
   3. tr 命令能用 \s 吗？
      绝对不行。

      tr 是一个非常老派且简单的工具，它不识别复杂的正则缩写（如 \d, \s, \w）。

      ❌ tr -d '\s' (这只会删除字母 s)

      ✅ tr -d '[:space:]' (这才会删除所有空白字符)

---

**总结对比表**
| 表达方式       | 含义               | 兼容性                          | 推荐场景                  |
| -------------- | ------------------ | ------------------------------- | ------------------------- |
| \s             | 单个空白字符       | 一般 (GNU 工具支持, BSD 不支持) | 快速在 Linux 命令行随手写 |
| [[:space:]]    | 单个空白字符       | 极高 (POSIX 标准)               | 编写正式脚本、跨平台使用  |
| ^$             | 纯空行             | 极高                            | 仅删除没有任何内容的行    |
| ^[[:space:]]*$ | 空行或仅含空格的行 | 极高                            | 清洗数据最常用的“吸尘器”  |

---

**练习5-C**

删除行尾的空格

答案
```shell
   sed 's/[[:space:]]*$//' notes.txt
```

---

**练习5-D**

中等
sed-4  只保留特定行范围
文件 data.txt 有10行，只打印第3行到第7行（其余行不显示）。
line1
line2
line3
line4
line5
line6
line7
line8
line9
line10
提示 查看答案
用行号范围 N,M 配合取反删除：删除1到2行，再删除8到10行。或者用 -n + p。
答案
sed -n '3,7p' data.txt
-n 禁止默认输出，3,7p 只打印第3到第7行。
另一种写法：sed '1,2d; 8,$d' data.txt

---

**练习5-11**


sed-5  在匹配行后插入内容
文件 hosts.txt 中，在每行含 localhost 的行后面插入一行 # local only。
127.0.0.1 localhost
192.168.1.1 gateway
::1 localhost
提示 查看答案
sed 的 a 命令（append）在匹配行之后插入文本。
答案
sed '/localhost/a # local only' hosts.txt
/localhost/a TEXT：找到含 localhost 的行，在其后追加 TEXT。
对应的 i 命令是在行前插入。


---



## 6. join - 合并两个有序文件

---

核心要求 & 用法
⚠️ join 要求两个输入文件都按连接键排好序！

选项	含义
-t 'SEP'	指定字段分隔符
-a 1	输出文件1中未匹配的行（左外连接）
-a 2	输出文件2中未匹配的行
-a 1 -a 2	全外连接
-o auto	自动决定输出字段（全外连接时必须）
-e 'STR'	缺失字段用 STR 填充


# 内连接：只输出两门课都有的学生
join -t'|' comp1511-sorted.psv comp2041-sorted.psv

# 全外连接：没选某课显示 --
join -t'|' -a1 -a2 -o auto -e'--' comp1511-sorted.psv comp2041-sorted.psv

# 内连接后按1511成绩升序、2041成绩降序排列
join -t'|' comp1511-sorted.psv comp2041-sorted.psv | sort -t'|' -k2,2 -k3,3r
练习 6-A
如果你只有两个未排序的成绩文件 a.psv 和 b.psv，用一条流水线完成排序+内连接（无需创建中间文件）。

查看答案

join -t'|' <(sort -t'|' -k1,1 a.psv) <(sort -t'|' -k1,1 b.psv)
使用 Bash 的进程替换 <(...)，把排序结果当作文件传给 join，无需中间文件。



---

## 综合练习： UNSW 校园数据处理实战

场景设定： 你拿到了一个名为 raw_data.txt 的原始文件，内容如下（包含脏数据）：


| 学号     | 课程     | 分数 | 日期       |
| -------- | -------- | ---- | ---------- |
| z5523839 | COMP9044 | 85   | 2026-03-01 |
| z1234567 | MATH1131 | 45   | 2026-02-15 |
| z9876543 | comp9044 | 92   | 2026-03-05 |
| z5523839 | COMP9417 | 78   | 2026-03-02 |
| z1111111 | ENGG1000 | 06   | 2026-01-20 |
| BAD_DATA | WRONG123 | XX   | 2026-00-00 |
| z2222222 | COMP9044 | 59   | 2026-03-10 |

---

### 第二部分：管道与过滤器 (Week 02) ###
4. 字段提取：使用 cut 提取所有的 zID 和分数（第一列和第三列）。注意：分隔符是 |（包含空格）。

5. 分数排序：

   首先，按 zID 升序排列。

   如果 zID 相同，按分数数字逆序排列。

6. 统计挂科率：统计所有分数低于 50 的学生人数。

7. 去重计数：统计这个文件里一共出现了多少门不同的课程（注意大小写统一）。

---


### 第三部分：进阶挑战（补全知识点）###
8. 前导零处理 (Sed)：最后一行 06 分，请用 sed 去掉分数前的 0，变成 6。

9. 权限检查 (ls -l)：在执行这些操作前，你需要确保 raw_data.txt 对你（Owner）是可读写的，但对其他人（Others）是不可见的。请写出修改权限的命令。

10. 简单计算 (Awk 入门)：计算所有 COMP9044 课程的平均分。（这是 Week 03 的核心，尝试自学一下 awk '{sum+=$3} END {print sum/NR}'）。

---